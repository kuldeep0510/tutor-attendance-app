# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# WhatsApp Client Configuration
# Set to true to enable verbose logging for WhatsApp client
WHATSAPP_DEBUG=false

# Cache and Auth Folders
# These folders will be created automatically
# WWEBJS_AUTH_FOLDER=.wwebjs_auth
# WWEBJS_CACHE_FOLDER=.wwebjs_cache

# Chrome/Chromium Path (Optional)
# If you need to specify a custom Chrome path for puppeteer
# CHROME_PATH=/path/to/chrome

# Optional: Set timeout values (in milliseconds)
# WHATSAPP_QR_TIMEOUT=60000
# WHATSAPP_SESSION_TIMEOUT=14400000 # 4 hours

# Security
# In production, generate a random string for this
SESSION_SECRET=your-session-secret-here

# Optional: Maximum file size for attachments (in bytes)
# MAX_FILE_SIZE=50000000 # 50MB
